### Book Service

Keeps track of all the books we have in the store. 

##### <u>Endpoints</u>

- /api/book/all - GET
  - Returns all books

- /api/book/create - POST
  - Creates a new book

- /api/book/`<slug>` - GET
  - Returns book matching the slug